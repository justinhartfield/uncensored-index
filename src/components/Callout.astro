---
interface Props { title?: string; warning?: boolean; }
const { title, warning = false } = Astro.props;
---
<aside class:list={['callout', { 'callout--warning': warning }]}>
  {title && <strong>{title}</strong>}
  <slot />
</aside>
