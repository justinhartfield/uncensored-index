---
import RankingPage from '../../components/RankingPage.astro';
import { models } from '../../data/models';
---
<RankingPage title="Best uncensored AI models" description="A controlled, evidence-gated comparison of uncensored text models." kicker="Overall model ranking" models={models} intro="No model receives a rank from reputation or vendor claims. Live scores publish only after the same protocol runs across the roster and a human review is complete." />
